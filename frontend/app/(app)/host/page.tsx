import { BecomeHostPage } from "@/components/host/become-host-page";

export const metadata = {
    title: "Become a Host | TripNest",
    description:
        "Switch your TripNest account to host mode, list properties, and manage bookings in a powerful host dashboard.",
};

export default function HostLandingPage() {
    return <BecomeHostPage />;
}

