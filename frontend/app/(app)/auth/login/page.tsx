import Login from "@/components/auth/login";

export const metadata = {
    title: "Login | TripNest",
    description: "Login to your TripNest account",
};

export default function LoginPage() {
    return <Login />;
}
